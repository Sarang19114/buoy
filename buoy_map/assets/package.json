{
  "name": "buoy_map",
  "private": true,
  "scripts": {
    "deploy": "esbuild app.js --bundle --minify --target=es2017 --outdir=../priv/static/assets"
  },
  "dependencies": {
    "esbuild": "^0.19.0",
    "maplibre-gl": "^3.6.2",
    "pmtiles": "^4.3.0"
  }
}
